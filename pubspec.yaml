name: ukil_chai
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 STATE MANAGEMENT
  provider: ^6.1.5+1

  # 🔥 HTTP FOR API CALLS
  http: ^1.2.2
  shared_preferences: ^2.5.5

  # 📸 IMAGE PICKER (ADD HERE)
  image_picker: ^1.1.2
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo/ukillogo.png